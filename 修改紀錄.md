# TK活動系統 — 修改紀錄

> 專案位置(本機):`/Users/haideechou/pdf-compare`
> GitHub:`https://github.com/hua77hua/pdf-compare`
> 線上網站:`https://tk244apple.pythonanywhere.com`
> 紀錄日期:2026-06-28

---

## 2026-07-19 新增「BTS 2026」分頁

| # | 項目 | 內容 |
|---|------|------|
| 1 | 新增「🎒 BTS 2026」分頁 | 排在「目前活動」後面。上半是**查詢框**、下半是**操盤 PDF 看板**(可上傳/覆蓋/瀏覽,做法同教育價) |
| 2 | 自動解析可搜尋清單 | 上傳 BTS 操盤 PDF 後,伺服器自動把 8 欄表格(分類/貨號/品名/型號/會員價/折讓額/促銷價/活動)讀成可查詢資料;換新檔即覆蓋 |
| 3 | 分詞搜尋 | 打「iPad Air 128G Wifi」不必照順序、忽略空格與「-」(Wi-Fi=Wifi),每個詞都命中才顯示;也可直接打貨號 |
| 4 | 贈品/分類正確歸屬 | PDF 的合併儲存格(分類、活動贈品)只帶給同機種的顏色/容量變體,不會誤套到後面的搭售配件;配件依貨號去重 |

搜尋結果顯示:分類・品名・**貨號(結帳用)**・會員價・促銷價・活動贈品。

**改了哪些檔案**:`app.py`(bts_products 資料表、`extract_bts_products` 解析、`/api/bts-products` 查詢與 rebuild、上傳時自動解析)、`static/index.html`(分頁與 view-bts 區塊)、`static/app.js`(查詢/上傳邏輯)、`static/style.css`(紫色配色與結果表欄位)。

> ⚠️ 限制:自動查詢只吃 **PDF**(需文字表格);**JPG 無法自動變成可搜尋資料**(伺服器無 OCR)。若操盤是圖片式 PDF 也會讀不到表格。

---

## 這次完成的改動（2026-06-28）

| # | 項目 | 內容 |
|---|------|------|
| 1 | 新增「目前活動」分頁 | 排在最前面、打開網站預設就看到。可上傳 PDF,下次上傳會自動覆蓋更新,手機/平板/電腦都能滑動瀏覽 |
| 2 | 分頁改名 | 「PDF 比較」→「活動比較」 |
| 3 | 分頁改名 | 「教育價活動」→「教育價貨號」 |
| 4 | 系統名稱 | 頂端大標題與瀏覽器分頁標題:「PDF 活動比較系統」→「TK活動系統」 |

目前分頁順序:**📢 目前活動 ・ 🔍 活動比較 ・ 🎓 教育價貨號 ・ 🏷️ 貨號表**

---

## 改了哪些檔案

- `app.py` — 啟用新類別 `current`(沿用既有的單一最新 PDF 上傳/覆蓋/讀取邏輯)
- `static/index.html` — 新增「目前活動」分頁與 PDF 看板區塊;各項名稱/標題修改
- `static/app.js` — 註冊 `current` 看板、切換分頁與開頁時載入
- `static/style.css` — 「目前活動」看板專屬橘紅配色

「目前活動」功能是複製原本「貨號表」那套成熟做法做的,行為完全一致。

---

## 如何讓線上網站更新(部署步驟)

每次在本機改完並推上 GitHub 後,到 PythonAnywhere 操作:

1. 登入 [pythonanywhere.com](https://www.pythonanywhere.com)
2. 上方 **Consoles** → 開一個 **Bash**
3. 執行:
   ```bash
   cd ~/pdf-compare
   git pull
   ```
4. 到上方 **Web** 分頁,按綠色 **Reload** 按鈕
5. 重新整理 `https://tk244apple.pythonanywhere.com` 即可看到更新

> 多個改動會在一次 `git pull` 時一起更新,不必分次。

---

## 日後想再改的話

1. 在本機 `/Users/haideechou/pdf-compare` 修改檔案
2. 推上 GitHub:
   ```bash
   cd ~/pdf-compare
   git add -A
   git commit -m "說明這次改了什麼"
   git push origin main
   ```
3. 依上面「部署步驟」在 PythonAnywhere `git pull` + Reload

---

## 本次 commit 紀錄

- `feat: 新增「目前活動」分頁,預設首頁可上傳/更新 PDF 供手機電腦瀏覽`
- `改:分頁名稱 PDF 比較 → 活動比較`
- `改:分頁名稱 教育價活動 → 教育價貨號`
- `改:系統名稱 PDF 活動比較系統 → TK活動系統`
